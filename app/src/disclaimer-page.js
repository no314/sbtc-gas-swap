// Style entry for the static disclaimer page. Loads the same design tokens and component
// recipes as the app plus a small page-local layer (app.css tail block).
import "./styles/tokens.css";
import "./styles/app.css";
